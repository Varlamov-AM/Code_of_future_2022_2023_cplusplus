#include <unordered_map>


int main(){

    std::unordered_map<KeyType, ValueType> ump;

    //
    // insert({k, v})
    // erase({k, v})
    // erase(iter)
    // find({k, v})
    // begin()
    // end()
    //

    // hash operation -> std::hash<key>

    return 0;
}
